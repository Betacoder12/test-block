function doSomething (num: number, num2: number, sprite: game.LedSprite) {
    led.plot(num, num2)
    if (_1 == "left") {
        if (sprite.get(LedSpriteProperty.X) == num && sprite.get(LedSpriteProperty.Y) == num2) {
        	
        }
    }
}
let _1 = ""
_1 = ""
basic.forever(function () {
	
})
